<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <style>
        .container{
            text-align: center;
        }
       p {
           font-size: 1.3rem ;
       }
    </style>
</head>
<body class="bg-dark text-white">
    <div class="container">

            <img src="https://image.tmdb.org/t/p/w500{{ movies.poster_path }}" alt="" class="img-thumbnail">
            <h2>{{ movies.title }}</h2>
            <p>{{ movies.overview }}</p>
            <p>Data de lançamento: {{ movies.release_date }}</p>
            <p>Média dos votos: {{ movies.vote_average|number_format(2, '.', ',') }}</p>

            <div>
                    <form action="">
                            <ul>
                                    <span class="text">Nome </span>
                                    <input class="form-control" type="text" name="txtNome" id="txtNome"/>

                                    <span>Comentario</span>
                                    <textarea class="form-control" name="txtComentario" id="txtArea"></textarea>

                                    <br/>

                                    <span class="button"></span>
                                    <input class="btn btn-primary" type="submit" name="btnSubmit" id="btnSubmit" value="Comentar" />
                            </ul>
                    </form>
            </div>
    </div>

</body>
</html>