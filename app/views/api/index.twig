<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TMDB</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body class="bg-black text-white">

<div class="container text-center">
    <div class="row justify-content-end">
        <div class="col-md-3 mt-3 mt-md-0">
            <form id="cityForm">
                <div class="input-group mb-3">
                    <input type="text" class="form-control" placeholder="Digite o nome do filme" name="name" value="{{ name }}">
                    <button class="btn btn-primary" type="submit">Buscar</button>
                </div>
            </form>
        </div>
    </div>

    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-4 g-4">
        {% for movie in movies %}
            <div class="col">
                <div class="card">
                    <img src="https://image.tmdb.org/t/p/w500{{ movie.poster_path }}" alt="{{ movie.title }}" class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title">{{ movie.title }}</h5>
                        <p class="card-text"><a href="/{{ movie.id }}" class="btn btn-primary">Detalhes</a></p>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>

    <div class="row mt-3">
        <div class="col text-center">
            {% if currentPage > 1 %}
                <a href="?name={{ name }}&page={{ currentPage - 1 }}" class="btn btn-secondary">Página Anterior</a>
            {% endif %}

            {% if currentPage < totalPages %}
                <a href="?name={{ name }}&page={{ currentPage + 1 }}" class="btn btn-secondary">Próxima Página</a>
            {% endif %}
        </div>
    </div>

    <br/>
</div>


</body>
</html>







